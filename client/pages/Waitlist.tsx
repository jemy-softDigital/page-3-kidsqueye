import PlaceholderPage from "./PlaceholderPage";

export default function Waitlist() {
  return (
    <PlaceholderPage 
      title="Waitlist Management" 
      description="Manage your enrollment waitlist, view applicant details, and handle enrollment processes."
    />
  );
}
